import type { PrincipleId } from "@/types";

/** Working principles, in the order they appear. Prose lives in translations. */
export const principles: PrincipleId[] = [
  "finish",
  "measure",
  "inherit",
  "ask",
];
